/** Use with `mx-auto` when a max-width is set, or text looks off-center in RTL. */
export const LANDING_SECTION_TITLE_CLASS =
  'w-full text-2xl font-extrabold tracking-tight sm:text-3xl lg:text-4xl'

/** Narrow copy blocks centered on small screens; align to start in side-by-side layouts at lg+. */
export const LANDING_HERO_COPY_WIDTH =
  'mx-auto w-full max-w-xl lg:mx-0 lg:max-w-none'
